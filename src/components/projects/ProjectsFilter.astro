---
import { ProjectsFilter as ProjectsFilterComponent } from "./ProjectsFilter";

interface Props {
  locale: "en" | "pl";
}

const { locale } = Astro.props;
---

<ProjectsFilterComponent locale={locale} client:load />
