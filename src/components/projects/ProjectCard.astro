---
import type { Project } from "@/i18n";

import { ProjectCardComponent } from "./ProjectCardComponent";

interface Props {
  project: Project;
  locale: "en" | "pl";
}

const { project, locale } = Astro.props;
---

<div
  class="project-card group relative overflow-hidden rounded-lg border border-border bg-card p-2 transition-all hover:border-primary"
  data-project-id={project.id}
  data-categories={project.categories.join(",")}
>
  <ProjectCardComponent project={project} locale={locale} client:visible />
</div>
