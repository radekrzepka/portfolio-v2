---
import { ArrowRight } from "lucide-react";

import { Button } from "@/components/ui/button";
import {
  COMMERCIAL_EXPERIENCE_YEARS,
  LEARNING_EXPERIENCE_YEARS,
} from "@/data/experience";
import { socialLinks } from "@/data/navigation";
import SectionContainer from "@/layouts/SectionContainer.astro";
---

<SectionContainer>
  <div class="mt-24 py-4">
    <div class="flex flex-col justify-center space-y-8">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold animate-slide-up">
        Cześć, jestem <span class="text-primary whitespace-nowrap"
          >Radosław Rzepka</span
        >
      </h1>
      <p
        class="text-xl text-muted-foreground max-w-[700px] animate-slide-up-delay"
      >
        Jestem full-stack developerem z {COMMERCIAL_EXPERIENCE_YEARS}-letnim
        doświadczeniem komercyjnym, a programowaniem zajmuję się od {
          LEARNING_EXPERIENCE_YEARS
        } lat. Na co dzień pracuję z React, Next.js, TypeScript i Node.js, tworząc
        rozwiązania, które łączą funkcjonalność z dobrym doświadczeniem użytkownika.
      </p>

      <div class="flex flex-col sm:flex-row gap-3 pt-4 animate-fade-in-delay">
        <a href="/projects">
          <Button variant="default" size="lg">
            Zobacz moje projekty <ArrowRight className="ml-2 h-4 w-4" />
          </Button>
        </a>
        <a href="/contact">
          <Button variant="outline" size="lg"> Skontaktuj się </Button>
        </a>
      </div>

      <div class="flex items-center gap-4 pt-2 animate-fade-in-delay-2">
        {
          socialLinks.map(({ href, icon: Icon, label }) => (
            <a
              href={href}
              class="text-muted-foreground hover:text-foreground transition-colors"
              target="_blank"
              rel="noopener noreferrer"
            >
              <Icon className="h-6 w-6" />
              <span class="sr-only">{label}</span>
            </a>
          ))
        }
      </div>
    </div>
  </div>
</SectionContainer>
